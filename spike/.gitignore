# Vendored toolchain (heavy, downloaded by `make wasi-sdk`)
vendor/

# mruby source clone — fetched by spike setup, not part of this repo
mruby/

# Build outputs
host/mruby-js.wasm
host/mruby-cmd.wasm
build/
# `make dist` 出力 — Phase A (bundle redistribution) 用、毎回再生成
dist/

# rake/mruby creates a lockfile next to MRUBY_CONFIG recording the mruby
# commit it built against — auto-generated, no need to track.
build_config/*.lock

# Editor
*.swp
.DS_Store
